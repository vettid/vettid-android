{
  "test_api_base_url": "https://tiqpij5mue.execute-api.us-east-1.amazonaws.com",
  "test_api_key": "vettid-test-key-dev-only",
  "test_user_prefix": "test_android_",
  "mock_attestation_enabled": true,
  "skip_attestation": true,
  "endpoints": {
    "health": "/test/health",
    "create_invitation": "/test/create-invitation",
    "cleanup": "/test/cleanup",
    "enroll_start": "/vault/enroll/start-direct",
    "enroll_set_password": "/vault/enroll/set-password",
    "enroll_finalize": "/vault/enroll/finalize"
  },
  "enrollment_notes": {
    "skip_attestation": "Set skip_attestation: true in enroll/start-direct request body",
    "device_type": "Set device_type: 'android' in enroll/start-direct request body",
    "invitation_flow": "Use invitation_code from create-invitation response",
    "public_endpoint": "/vault/enroll/start-direct does NOT require Authorization header"
  }
}
